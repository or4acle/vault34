# --- Model assets: fetched once by setup_assets.py, never committed -------
# wd14.onnx is ~311 MB; GitHub hard-rejects files over 100 MB.
# Run `python setup_assets.py` after cloning.
models/

# --- Your media: private, and regenerated by the ingest pipeline -----------
media/

# --- Local database and per-machine settings -------------------------------
*.db
*.db-wal
*.db-shm
config.json
bin/

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# --- Tooling / logs -------------------------------------------------------
*.log
.DS_Store
Thumbs.db
.vscode/
.idea/
