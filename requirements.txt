# Vault34 runtime dependencies.
# Install with:  python -m pip install -r requirements.txt
#
# Pinned to the versions this build was verified against (Python 3.14).

# --- tagging (SmilingWolf WD14 tagger, ONNX) ---------------------------------
onnxruntime==1.28.0        # CPU by default; add onnxruntime-gpu for CUDA
numpy==2.5.1

# --- media decoding / thumbnails ---------------------------------------------
pillow==12.3.0             # images, GIFs, phash/dhash, thumbnails
opencv-python-headless==5.0.0.93   # video probe + frame extraction fallback

# --- filesystem watching ------------------------------------------------------
watchdog==6.0.0

# --- local HTTP API + window --------------------------------------------------
Flask==3.1.3
Werkzeug==3.1.8
pywebview==6.2.1           # native window; safe to omit (falls back to browser)

# --- setup-time only (downloads the model once) -------------------------------
requests==2.34.2           # used by setup_assets.py
tqdm==4.69.1               # download progress bar
onnx==1.23.0               # model graph inspection in setup_assets.py --check
